int main(int argc, const char** argv) {
    // 1. The server generates a map
    // 2. The server waits for two connections
    // 3. The game starts
    // 4. The server receives the actions from both player
    // 5. The server regularly send current game state to both player
}